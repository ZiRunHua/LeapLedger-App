part of 'enter.dart';

class TotalHeader extends StatelessWidget {
  const TotalHeader({super.key, required this.type});
  final IncomeExpense type;
  @override
  Widget build(BuildContext context) {
    return Row(
      children: [],
    );
  }
}
